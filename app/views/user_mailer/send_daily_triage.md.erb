Hello @<%= @user.github %>,

<% if @days > @max_days %>
It's been [<%= @days %> days] since you've clicked on a CodeTriage.com link, let's keep those issue counts down.
<% end %>
Here are some issues you should check out:

<% @sub_hashes.each do |id, sub_hash| %>

##<%= sub_hash[:repo].full_name %>

<% if sub_hash[:assignments].present? %>
### Issues
<% end %>

<% sub_hash[:assignments].each do |assignment| %>
**<%= link_to("#{sub_hash[:repo].full_name}##{assignment.issue.number}", issue_click_url(assignment.id, assignment.user.id )) if assignment.issue.number %>** <%= assignment.issue.title %> (<%= time_ago_in_words assignment.issue.last_touched_at %> ago)

<% end %>

<% if sub_hash[:read_docs].present? || sub_hash[:write_docs].present? %>
### Docs
<% end %>

<% sub_hash[:read_docs].each do |doc| %>
⇢ [<%= doc.path %>](<%= doc_method_url doc %>) ([source](<%= doc.to_github %>))

<%= doc.doc_comments.first.try(:md_safe_comment_block) %>

<% end %>
<% sub_hash[:write_docs].each do |doc| %>
⇢ [<%= doc.path %>](<%= doc_method_url doc %>) ([source](<%= doc.to_github %>))

~~~
<%= DocMethod::NeedsDocs %>
~~~

<% end %>

---
<% end %>

Go forth and make the world a better place!

Sincerly,

[Schneems](https://www.schneems.com)

[Help triage more repos at CodeTriage.com](<%= root_url %>)

[Remove Account](<%= token_delete_user_url(@user.account_delete_token) %>)
